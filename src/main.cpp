#include "Automata.h"

int main() {
    Automata automat1;
    automat1.on();
    automat1.coin(120);
    automat1.getMenu();
    automat1.choice(1);
    automat1.check();
    automat1.cook();
    automat1.finish();
    
    return 0;
}